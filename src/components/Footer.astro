---
import { Icon } from 'astro-icon/components';
const year = new Date().getFullYear();
---
<footer class="footer">
  <div class="footer__inner">
    <a href="#home" class="footer__logo">
      <img src="/logo-dark.webp" alt="Fivezzza Pizzeria" width="200" height="140" loading="lazy" />
    </a>

    <a
      href="https://www.instagram.com/fivezzza/"
      target="_blank"
      rel="noopener noreferrer"
      class="footer__ig"
      aria-label="Fivezzza sur Instagram"
    >
      <Icon name="simple-icons:instagram" class="footer__ig-icon" />
      <span>@fivezzza</span>
    </a>

    <nav class="footer__nav" aria-label="Footer navigation">
      <a href="#menu">La Carte</a>
      <span aria-hidden="true">·</span>
      <a href="#about">Notre Histoire</a>
      <span aria-hidden="true">·</span>
      <a href="#hours">Horaires</a>
    </nav>
    <p class="footer__copy">&copy; {year} Fivezzza Pizzeria. Tous droits réservés.</p>
  </div>
</footer>

<style>
  .footer {
    background: var(--dark);
    color: var(--cream);
    text-align: center;
  }
  .footer__inner {
    max-width: 800px;
    margin: 0 auto;
    padding: 4rem 2rem 3rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
  }
  .footer__logo img {
    width: 180px;
    height: auto;
    object-fit: contain;
    opacity: 0.9;
    transition: opacity 0.2s;
  }
  .footer__logo:hover img { opacity: 1; }

  /* Instagram button */
  .footer__ig {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.6rem 1.4rem;
    border: 1.5px solid rgba(245,237,216,0.2);
    color: var(--cream);
    text-decoration: none;
    font-size: 0.85rem;
    font-weight: 600;
    letter-spacing: 0.06em;
    transition: border-color 0.2s, background 0.2s, color 0.2s;
    margin: 0.25rem 0;
  }
  .footer__ig:hover {
    border-color: var(--red);
    background: var(--red);
    color: var(--cream);
  }
  .footer__ig-icon {
    width: 1.1rem;
    height: 1.1rem;
    flex-shrink: 0;
  }

  .footer__nav {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    flex-wrap: wrap;
    justify-content: center;
  }
  .footer__nav a {
    font-size: 0.8rem;
    font-weight: 600;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: rgba(245,237,216,0.7);
    text-decoration: none;
    transition: color 0.2s;
  }
  .footer__nav a:hover { color: var(--red); }
  .footer__nav span { color: rgba(245,237,216,0.25); }

  .footer__copy {
    font-size: 0.75rem;
    color: #8C8696; /* 4.9:1 on --dark */
    margin-top: 0.5rem;
  }

  @media (max-width: 540px) {
    .footer__inner { padding: 3rem 1.5rem 2rem; gap: 0.75rem; }
    .footer__logo img { width: 140px; }
    .footer__nav { gap: 0.5rem; }
  }
</style>
