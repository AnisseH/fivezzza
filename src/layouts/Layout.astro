---
interface Props {
  title?: string;
  description?: string;
}
const {
  title = 'Fivezzza Pizzeria | Pizza Artisanale à Lille',
  description = 'Fivezzza, pizzeria artisanale à Lille. Pâte maison levée 48h, ingrédients frais sélectionnés. Ouvert midi et soir — Pass. de l\'Internationale, 59800 Lille.',
} = Astro.props;

const siteUrl = 'https://fivezzza.pages.dev';
const ogImage = `${siteUrl}/logo.jpeg`;

const jsonLd = {
  '@context': 'https://schema.org',
  '@type': 'Restaurant',
  name: 'Fivezzza Pizzeria',
  image: ogImage,
  url: siteUrl,
  address: {
    '@type': 'PostalAddress',
    streetAddress: "Pass. de l'Internationale",
    addressLocality: 'Lille',
    postalCode: '59800',
    addressCountry: 'FR',
  },
  geo: {
    '@type': 'GeoCoordinates',
    latitude: 50.6276912,
    longitude: 3.0927066,
  },
  openingHoursSpecification: [
    {
      '@type': 'OpeningHoursSpecification',
      dayOfWeek: ['Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday', 'Sunday'],
      opens: '12:00',
      closes: '14:00',
    },
    {
      '@type': 'OpeningHoursSpecification',
      dayOfWeek: ['Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday', 'Sunday'],
      opens: '19:00',
      closes: '22:00',
    },
  ],
  servesCuisine: ['Pizza', 'Italian'],
  priceRange: '€€',
  sameAs: ['https://www.instagram.com/fivezzza/'],
};
---
<!doctype html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <!-- Primary SEO -->
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={siteUrl} />

    <!-- Open Graph (social sharing) -->
    <meta property="og:type" content="restaurant" />
    <meta property="og:site_name" content="Fivezzza Pizzeria" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={siteUrl} />
    <meta property="og:image" content={ogImage} />
    <meta property="og:image:alt" content="Logo Fivezzza Pizzeria" />
    <meta property="og:locale" content="fr_FR" />

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogImage} />

    <!-- Local business structured data -->
    <script type="application/ld+json" set:html={JSON.stringify(jsonLd)} />

    <!-- Favicon -->
    <link rel="icon" type="image/jpeg" href="/logo.jpeg" />

    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Playfair+Display:ital,wght@0,400;0,700;1,400&family=DM+Sans:opsz,wght@9..40,400;9..40,500;9..40,600&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <slot />
  </body>
</html>

<style is:global>
  :root {
    --red:   #E0442C;
    --red-d: #C23820;
    --blue:  #2855B5;
    --blue-l:#4070D0;
    --cream: #F5EDD8;
    --dark:  #1A1720;
    --grey:  #2E2A38;
  }

  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

  html { scroll-behavior: smooth; }

  body {
    background: var(--cream);
    color: var(--dark);
    font-family: 'DM Sans', sans-serif;
    overflow-x: hidden;
  }

  /* Checkered divider */
  .checker {
    height: 28px;
    background-image: repeating-conic-gradient(var(--red) 0% 25%, var(--cream) 0% 50%);
    background-size: 28px 28px;
  }
  .checker--blue {
    background-image: repeating-conic-gradient(var(--blue) 0% 25%, var(--cream) 0% 50%);
  }
  .checker--dark {
    background-image: repeating-conic-gradient(var(--dark) 0% 25%, var(--red) 0% 50%);
    height: 20px;
  }

  /* Shared button */
  .btn {
    display: inline-block;
    padding: 0.875rem 2.5rem;
    font-family: 'DM Sans', sans-serif;
    font-size: 0.85rem;
    font-weight: 600;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    text-decoration: none;
    border: 2px solid transparent;
    cursor: pointer;
    transition: background 0.2s, color 0.2s, transform 0.15s;
  }
  .btn--red  { background: var(--red);  color: var(--cream); border-color: var(--red); }
  .btn--red:hover  { background: var(--red-d); border-color: var(--red-d); transform: translateY(-2px); }
  .btn--outline { background: transparent; color: var(--cream); border-color: var(--cream); }
  .btn--outline:hover { background: var(--cream); color: var(--dark); }
</style>
